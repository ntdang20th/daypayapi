receivers:
  otlp:
    protocols:
      http:
      grpc:


exporters:
  elasticsearch:
    endpoints: [ "http://elasticsearch:9200" ]
    index: "otel-logs"

service:
  pipelines:
    logs:
      receivers: [ otlp ]
      exporters: [ elasticsearch ]
